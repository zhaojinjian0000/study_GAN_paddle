# 创建一个项目
```bash
# 创建github仓库
# 在github直接点新建一个repository
git clone https://github.com/zhaojinjian0000/study_GAN_paddle # 下载下来

# 上传代码
git add .
git commit -m '初始化项目'
git push origin main
# 这里vscode的git拓展会自动帮助输入账号密码登录
```

# 每次更新
```bash
git add .
git commit -m '更新内容'
git push origin main
```